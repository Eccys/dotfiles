# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Decoration and Animations Settings

# Hyprland Wiki Links
# Animation - https://wiki.hyprland.org/Configuring/Animations/
# Decoration - https://wiki.hyprland.org/Configuring/Variables/#decoration

# Sourcing colors generated by wallust
source = $HOME/.config/hypr/wallust/wallust-hyprland.conf

decoration {
  rounding = 10
         
  active_opacity = 1.0
  inactive_opacity = 0.9
  fullscreen_opacity = 1.0

  dim_inactive = true
  dim_strength = 0.1
  dim_special = 0.4

  shadow {
    enabled = true
    range = 2
    render_power = 2
    
    color = $color12
    color_inactive = $color2
}

  blur {
    enabled = true	
    size = 6
    passes = 2
    ignore_opacity = true
    new_optimizations = true
    special = true
  }
}

# animations {
#     enabled = yes    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
#     bezier = myBezier, 0.10, 0.9, 0.1, 1.05
#     bezier = wind, 0.05, 0.9, 0.1, 1.05
#     bezier = winIn, 0.1, 1.1, 0.1, 1.1
#     bezier = winOut, 0.3, -0.3, 0, 1
#     bezier = liner, 1, 1, 1, 1
#     bezier = overshot, 0.05, 0.9, 0.1, 1.05
#     bezier = smoothOut, 0.5, 0, 0.99, 0.99
#     bezier = smoothIn, 0.5, -0.5, 0.68, 1.5
#     bezier = smoothBezier, 0.25, 0.9, 0.35, 1.0      # Quick ease-in-out for fluidity
#     bezier = overshotBezier, 0.1, 1.2, 0.15, 1.05    # Snappy overshoot for flair
#     bezier = quickSnap, 0.45, 0.85, 0.6, 1.0        # Fast start, smooth finish
#     bezier = gentleEnd, 0.2, 0.95, 0.4, 1.0         # Soft end, fixed to complete the slide
#     bezier = workspaceFlow, 0.15, 1.25, 0.5, 0.95   # Dramatic ease-in, overshoot, smooth out
#     bezier = bounceSlide, 0.2, 1.3, 0.3, 1.0        # Bounce effect for window movement
#     bezier = linear, 1.0, 1.0, 1.0, 1.0             # Linear for subtle effects
#     bezier = reversePop, 0.9, -0.2, 0.85, 0.0       # Quick scale-down, stops short
#
#     animation = windows, 1, 5, bounceSlide, slide     # Bounce + slide for existing window movement
#     animation = windowsOut, 1, 3, reversePop, popin   # Brief reverse pop-in, no darkening
#     animation = windowsIn, 1, 3, overshotBezier, popin  # Bold pop-in for new windows
#     animation = windowsMove, 1, 4, wind, slide
#     animation = border, 1, 10, default
#     animation = border, 1, 1, liner
#     animation = borderangle, 1, 180, liner, loop # used by rainbow borders and rotating colors
#     animation = fade, 1, 3, linear                   # Fast, no opacity drop for windowsOut
#     animation = workspaces, 1, 6, default
#     animation = workspaces, 1, 5, overshot
#     # animations for -git or version >0.42.0
#     # animation = workspacesIn, 1, 5, winIn, slide
#     # animation = workspacesOut, 1, 5, winOut, slide = workspacesOut, 1, 5, winOut, slide
# }

# animations {
# enabled = yes
# 
#     # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
#     bezier = myBezier, 0.10, 0.9, 0.1, 1.05
# 
#     animation = windows, 1, 5, myBezier, slide
#     animation = windowsOut, 1, 5, myBezier, slide
#     animation = border, 1, 10, default
#     animation = fade, 1, 7, default
#     animation = workspaces, 1, 6, default
# }
    
# animations (new) {
#   enabled = yes
# 
#   bezier = wind, 0.05, 0.9, 0.1, 1.05
#   bezier = winIn, 0.1, 1.1, 0.1, 1.1
#   bezier = winOut, 0.3, -0.3, 0, 1
#   bezier = liner, 1, 1, 1, 1
#   bezier = overshot, 0.05, 0.9, 0.1, 1.05
#   bezier = smoothOut, 0.5, 0, 0.99, 0.99
#   bezier = smoothIn, 0.5, -0.5, 0.68, 1.5
#   
#   animation = windows, 1, 6, wind, slide
#   animation = windowsIn, 1, 5, winIn, slide
#   animation = windowsOut, 1, 3, smoothOut, slide
#   animation = windowsMove, 1, 5, wind, slide
#   animation = border, 1, 1, liner
#   animation = borderangle, 1, 180, liner, loop #used by rainbow borders and rotating colors
#   animation = fade, 1, 3, smoothOut
#   animation = workspaces, 1, 5, overshot
#   
#   # animations for -git or version >0.42.0
#   #animation = workspacesIn, 1, 5, winIn, slide
#   #animation = workspacesOut, 1, 5, winOut, slide
# }
